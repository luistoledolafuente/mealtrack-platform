# Environment variables
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mealtrack

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# App
API_PREFIX=/api/v1
CORS_ORIGIN=*
