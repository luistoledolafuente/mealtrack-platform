name: mealtrack
description: MealTrack - mobile-first meal plan management platform
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Network
  http: ^1.1.0

  # Local storage
  shared_preferences: ^2.2.2

  # State management (TODO: choose provider/riverpod/bloc)
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
